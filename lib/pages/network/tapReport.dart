import 'package:flutter/material.dart';

class TapReportePage extends StatelessWidget {
  const TapReportePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('TapReportePage'),
      ),
      body: Center(
        child: const Text('TapReportePage'),
      ),
    );
  }
}
